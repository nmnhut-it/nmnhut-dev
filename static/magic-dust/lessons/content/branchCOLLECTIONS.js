import { codeTask, learningBranch } from "./python-path-builders.js";

const task = config => codeTask(config);
const cells = [
  { intro: { title: "✦ NHÁNH DỤNG CỤ COLLECTION ✦", hook: "Kho đồ đang lộn xộn. Bạn sẽ học cách thêm, xóa, sao chép, sắp xếp, tính nhanh và kiểm tra dữ liệu trong list lẫn chuỗi.", art: "assets/future-machine.webp" } },
  { quiz: { title: "Ôn list", questions: [
    { q: "Cho `bag = [\"key\", \"map\", \"gem\"]`. Biểu thức nào lấy phần tử cuối?", a: ["`bag[-1]`", "`bag[0]`", "`bag[3]`"], correct: 0 },
  ] } },
  { npc: "`append(value)` thêm đúng một phần tử vào cuối list. Nếu `value` cũng là list, cả list đó trở thành một phần tử lồng bên trong." },
  ...task({ label: "collections_append_demo.py", starter: "bag = [\"key\", \"map\"]\nbag.append(\"gem\")\nprint(bag)\n", note: "RUN KIỂM CHỨNG\nCho sẵn list có `key`, `map`; không có INPUT. PROCESS: dùng `append()` thêm một chuỗi vào cuối. OUTPUT là `['key', 'map', 'gem']`.", expectOut: /^\['key', 'map', 'gem'\]$/, solution: "bag = [\"key\", \"map\"]\nbag.append(\"gem\")\nprint(bag)\n" }),
  { npc: "`extend(items)` lấy từng phần tử từ `items` và nối chúng vào cuối list. Vì vậy `bag.extend([\"gem\", \"coin\"])` thêm hai phần tử, không tạo list lồng." },
  ...task({ label: "collections_extend_fix.py", starter: "bag = [\"key\"]\nbag.append([\"gem\", \"coin\"])\nprint(bag)\n", note: "ĐỀ BÀI\nCho sẵn `bag = [\"key\"]`; không có INPUT. Sửa `append()` thành công cụ thêm từng phần tử của list mới. OUTPUT đúng là `['key', 'gem', 'coin']`.", expectOut: /^\['key', 'gem', 'coin'\]$/, solution: "bag = [\"key\"]\nbag.extend([\"gem\", \"coin\"])\nprint(bag)\n" }),
  { npc: "`insert(index, value)` chèn một phần tử vào đúng index; các phần tử từ vị trí đó trở đi dịch sang phải. Ví dụ `bag.insert(1, \"map\")` đặt `map` vào index 1." },
  ...task({ label: "collections_insert.py", starter: "bag = [\"key\", \"gem\"]\nbag.insert(1, \"map\")\nprint(bag)\n", note: "RUN KIỂM CHỨNG\nCho sẵn list `key`, `gem`; không có INPUT. PROCESS: chèn `map` tại index 1. OUTPUT là `['key', 'map', 'gem']`.", expectOut: /^\['key', 'map', 'gem'\]$/, solution: "bag = [\"key\", \"gem\"]\nbag.insert(1, \"map\")\nprint(bag)\n" }),
  { checkpoint: { text: "`append(x)` thêm một phần tử; `extend(items)` nối từng phần tử; `insert(index, x)` chèn `x` tại index đã chọn." } },
  { quiz: { title: "Chọn cách thêm list", questions: [
    { q: "Cho `bag = [\"key\"]`. Muốn kết quả là `[\"key\", \"map\", \"gem\"]` từ list `more = [\"map\", \"gem\"]`, dùng dòng nào?", a: ["`bag.extend(more)`", "`bag.append(more)`", "`bag.insert(more)`"], correct: 0 },
  ] } },
  { npc: "`pop()` vừa xóa vừa trả về phần tử cuối. `pop(index)` làm tương tự ở index đã chọn, nên có thể gán phần tử lấy ra vào một biến." },
  ...task({ label: "collections_pop_demo.py", starter: "queue = [\"An\", \"Bình\", \"Chi\"]\nlast = queue.pop()\nfirst = queue.pop(0)\nprint(last)\nprint(first)\nprint(queue)\n", note: "RUN KIỂM CHỨNG\nCho sẵn hàng đợi ba tên; không có INPUT. PROCESS: `pop()` lấy cuối, `pop(0)` lấy đầu. OUTPUT lần lượt là `Chi`, `An`, `['Bình']`.", expectOut: { all: [{ minLines: 3 }, /^Chi$/, /^An$/, /^\['Bình'\]$/] }, solution: "queue = [\"An\", \"Bình\", \"Chi\"]\nlast = queue.pop()\nfirst = queue.pop(0)\nprint(last)\nprint(first)\nprint(queue)\n" }),
  { npc: "`remove(value)` xóa lần xuất hiện đầu tiên của một giá trị. Nó tìm theo giá trị, khác với `pop(index)` tìm theo vị trí." },
  ...task({ label: "collections_remove_fix.py", starter: "colors = [\"red\", \"blue\", \"red\"]\ncolors.pop(\"red\")\nprint(colors)\n", note: "ĐỀ BÀI\nCho sẵn list có hai chuỗi `red`; không có INPUT. Sửa lệnh để xóa lần xuất hiện đầu tiên theo giá trị. OUTPUT đúng là `['blue', 'red']`.", expectOut: /^\['blue', 'red'\]$/, solution: "colors = [\"red\", \"blue\", \"red\"]\ncolors.remove(\"red\")\nprint(colors)\n" }),
  { npc: "`clear()` xóa toàn bộ phần tử nhưng vẫn giữ chính list đó. Sau `bag.clear()`, biến `bag` giữ list rỗng `[]`." },
  ...task({ label: "collections_clear.py", starter: "bag = [1, 2, 3]\nbag.clear()\nprint(bag)\n", note: "RUN KIỂM CHỨNG\nCho sẵn list ba số; không có INPUT. PROCESS: dùng `clear()` xóa mọi phần tử. OUTPUT là `[]`.", expectOut: /^\[\]$/, solution: "bag = [1, 2, 3]\nbag.clear()\nprint(bag)\n" }),
  { checkpoint: { text: "`pop(index)` lấy và xóa theo vị trí; `remove(value)` xóa lần xuất hiện đầu tiên theo giá trị; `clear()` xóa toàn bộ phần tử." } },
  { quiz: { title: "Xóa phần tử đúng cách", questions: [
    { q: "Cho `items = [10, 20, 30]`. Dòng nào lấy số 20 ra khỏi list và gán nó vào `picked`?", a: ["`picked = items.pop(1)`", "`picked = items.remove(20)`", "`picked = items.clear()`"], correct: 0 },
  ] } },
  { npc: "`count(value)` đếm số lần xuất hiện; `index(value)` trả về index đầu tiên. Chỉ gọi `index()` khi biết giá trị có trong list, vì giá trị vắng mặt sẽ làm máy báo lỗi." },
  ...task({ label: "collections_count_index.py", starter: "items = [\"key\", \"map\", \"key\"]\nprint(items.count(\"key\"))\nif \"map\" in items:\n    print(items.index(\"map\"))\n", note: "RUN KIỂM CHỨNG\nCho sẵn list ba chuỗi; không có INPUT. PROCESS: đếm `key`, dùng `in` kiểm tra rồi tìm index của `map`. OUTPUT là `2` rồi `1`.", expectOut: { all: [{ minLines: 2 }, /^2$/, /^1$/] }, solution: "items = [\"key\", \"map\", \"key\"]\nprint(items.count(\"key\"))\nif \"map\" in items:\n    print(items.index(\"map\"))\n" }),
  { npc: "`copy()` tạo một list riêng có cùng phần tử. Sửa list mới không làm thay đổi các ô cấp ngoài của list ban đầu." },
  ...task({ label: "collections_copy.py", starter: "original = [1, 2, 3]\nchanged = original.copy()\nchanged.append(4)\nprint(original)\nprint(changed)\n", note: "RUN KIỂM CHỨNG\nCho sẵn list 1, 2, 3; không có INPUT. PROCESS: copy rồi chỉ thêm 4 vào bản copy. OUTPUT là `[1, 2, 3]` rồi `[1, 2, 3, 4]`.", expectOut: { all: [{ minLines: 2 }, /^\[1, 2, 3\]$/, /^\[1, 2, 3, 4\]$/] }, solution: "original = [1, 2, 3]\nchanged = original.copy()\nchanged.append(4)\nprint(original)\nprint(changed)\n" }),
  { npc: "`sort()` sắp xếp chính list hiện tại; `reverse()` đảo thứ tự chính list hiện tại. `sorted(items)` tạo list mới đã sắp xếp và giữ `items` nguyên vẹn." },
  ...task({ label: "collections_sort_sorted.py", starter: "scores = [7, 2, 9]\nordered = sorted(scores)\nprint(scores)\nprint(ordered)\nscores.sort()\nscores.reverse()\nprint(scores)\n", note: "RUN KIỂM CHỨNG\nCho sẵn ba điểm; không có INPUT. PROCESS: tạo bản sắp xếp bằng `sorted()`, sau đó sắp xếp và đảo chính list gốc. OUTPUT là `[7, 2, 9]`, `[2, 7, 9]`, `[9, 7, 2]`.", expectOut: { all: [{ minLines: 3 }, /^\[7, 2, 9\]$/, /^\[2, 7, 9\]$/, /^\[9, 7, 2\]$/] }, solution: "scores = [7, 2, 9]\nordered = sorted(scores)\nprint(scores)\nprint(ordered)\nscores.sort()\nscores.reverse()\nprint(scores)\n" }),
  { npc: "Các lệnh có sẵn `sum(items)`, `min(items)` và `max(items)` lần lượt tính tổng, lấy giá trị nhỏ nhất và lớn nhất của list số." },
  ...task({ label: "collections_aggregates.py", starter: "scores = [6, 9, 7, 8]\nprint(sum(scores))\nprint(min(scores))\nprint(max(scores))\n", note: "RUN KIỂM CHỨNG\nCho sẵn bốn điểm; không có INPUT. PROCESS: dùng ba lệnh tổng hợp. OUTPUT lần lượt là `30`, `6`, `9`.", expectOut: { all: [{ minLines: 3 }, /^30$/, /^6$/, /^9$/] }, solution: "scores = [6, 9, 7, 8]\nprint(sum(scores))\nprint(min(scores))\nprint(max(scores))\n" }),
  { npc: "`enumerate(items)` cho mỗi lượt cả index lẫn phần tử. Mẫu `for index, value in enumerate(items):` giúp in số thứ tự mà không phải tự tăng biến đếm." },
  ...task({ label: "collections_enumerate.py", starter: "names = [\"An\", \"Bình\", \"Chi\"]\nfor index, name in enumerate(names):\n    print(index, name)\n", note: "RUN KIỂM CHỨNG\nCho sẵn ba tên; không có INPUT. PROCESS: dùng `enumerate()` để nhận index và tên cùng lúc. OUTPUT là `0 An`, `1 Bình`, `2 Chi`.", expectOut: { all: [{ minLines: 3 }, /^0 An$/, /^1 Bình$/, /^2 Chi$/] }, solution: "names = [\"An\", \"Bình\", \"Chi\"]\nfor index, name in enumerate(names):\n    print(index, name)\n" }),
  { checkpoint: { text: "`count` và `index` tra cứu; `copy` tạo list riêng; `sort`/`reverse` sửa list hiện tại; `sorted` tạo list mới. `sum`, `min`, `max` tổng hợp số; `enumerate` cung cấp index và phần tử." } },
  { quiz: { title: "Dụng cụ đọc và sắp xếp", questions: [
    { q: "Cho `scores = [8, 3, 6]`. Muốn có `ordered = [3, 6, 8]` mà vẫn giữ nguyên `scores`, dùng dòng nào?", a: ["`ordered = sorted(scores)`", "`ordered = scores.sort()`", "`ordered = scores.reverse()`"], correct: 0 },
    { q: "Cho `values = [4, 9, 2]`. Biểu thức nào cho giá trị 9?", a: ["`max(values)`", "`min(values)`", "`sum(values)`"], correct: 0 },
  ] } },
  { npc: "Với chuỗi, `isdigit()` kiểm tra mọi ký tự đều là chữ số; `isalpha()` kiểm tra mọi ký tự đều là chữ cái. Chuỗi rỗng cho `False` ở cả hai phép kiểm tra." },
  ...task({ label: "collections_string_kinds.py", starter: "code = \"12345\"\nname = \"Kotopia\"\nprint(code.isdigit())\nprint(name.isalpha())\nprint(\"12A\".isdigit())\n", note: "RUN KIỂM CHỨNG\nCho sẵn ba chuỗi; không có INPUT. PROCESS: kiểm tra nhóm ký tự bằng `isdigit()` và `isalpha()`. OUTPUT là `True`, `True`, `False`.", expectOut: { all: [{ minLines: 3 }, /^True$/, /^False$/] }, solution: "code = \"12345\"\nname = \"Kotopia\"\nprint(code.isdigit())\nprint(name.isalpha())\nprint(\"12A\".isdigit())\n" }),
  { npc: "`startswith(prefix)` kiểm tra phần đầu chuỗi; `endswith(suffix)` kiểm tra phần cuối. Hai lệnh trả về `True` hoặc `False` và có phân biệt chữ hoa, chữ thường." },
  ...task({ label: "collections_string_edges.py", starter: "filename = \"report.py\"\nprint(filename.startswith(\"rep\"))\nprint(filename.endswith(\".py\"))\n", note: "RUN KIỂM CHỨNG\nCho sẵn tên file `report.py`; không có INPUT. PROCESS: kiểm tra tiền tố `rep` và hậu tố `.py`. Hai dòng OUTPUT đều là `True`.", expectOut: { all: [{ minLines: 2 }, /^True$/] }, solution: "filename = \"report.py\"\nprint(filename.startswith(\"rep\"))\nprint(filename.endswith(\".py\"))\n" }),
  ...task({ label: "collections_validate_code.py", starter: "code = \"MD-2048\"\nnumber_part = code[3:]\n# Kiểm tra đầu MD- và phần sau chỉ có chữ số.\n", note: "XƯỞNG PHÉP\nCho sẵn `code = \"MD-2048\"`; không có INPUT. PROCESS: dùng `startswith()` với `MD-`, dùng `isdigit()` cho `number_part`, ghép hai điều kiện bằng `and`. OUTPUT phải là `VALID`.", expectOut: /^VALID$/, solution: "code = \"MD-2048\"\nnumber_part = code[3:]\nif code.startswith(\"MD-\") and number_part.isdigit():\n    print(\"VALID\")\nelse:\n    print(\"INVALID\")\n" }),
  { checkpoint: { text: "`isdigit()` kiểm tra chuỗi chỉ có chữ số; `isalpha()` kiểm tra chuỗi chỉ có chữ cái; `startswith()` và `endswith()` kiểm tra phần đầu hoặc cuối chuỗi." } },
  { quiz: { title: "Kiểm tra chuỗi", questions: [
    { q: "Cho `filename = \"photo.png\"`. Điều kiện nào đúng?", a: ["`filename.endswith(\".png\")`", "`filename.isdigit()`", "`filename.startswith(\"png\")`"], correct: 0 },
    { q: "Chuỗi nào làm `text.isalpha()` trả về `False`?", a: ["`\"Koto2\"`", "`\"Kotopia\"`", "`\"Pip\"`"], correct: 0 },
  ] } },
];

export default learningBranch({ id: "branchCOLLECTIONS", title: "NHÁNH DỤNG CỤ COLLECTION", subtitle: "các hàm list, lệnh tổng hợp và phép kiểm tra chuỗi", machineName: "MÁY XẾP KHO DỮ LIỆU", machineBlurb: "thêm, xóa, sao chép, sắp xếp, tổng hợp và kiểm tra list hoặc chuỗi", cells });
