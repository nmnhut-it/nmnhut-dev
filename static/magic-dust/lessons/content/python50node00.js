import { code, makePython50Lesson } from "./python50-template.js";
const common = ["from old_computer import say_num", "", "first = 14", "second = 5", "command = 1", "", "if command == 1:", "    result = first + second", "elif command == 2:", "    result = first - second", "", "say_num(result)"];
export default makePython50Lesson(0, {
  subtitle: "chọn phép cộng hoặc trừ bằng if / elif",
  machineName: "MÁY TÍNH HAI LỆNH", machineBlurb: "đọc mã lệnh rồi chọn đúng phép tính",
  intro: "Chiếc máy nhận một mã rồi chọn phép tính tương ứng. Chặng này dùng mã 1 cho phép cộng và mã 2 cho phép trừ.",
  prior: ["Cho `command = 2`. Với nhánh `if command == 1` đứng trước nhánh `elif command == 2`, nhánh nào chạy?", ["`elif command == 2`", "`if command == 1`", "Không nhánh nào"]],
  teach: "`command` là mã cho sẵn: 1 nghĩa là cộng, 2 nghĩa là trừ. Máy so từng điều kiện và chỉ chạy nhánh khớp.",
  demo: { starter: code(common), label: "p50_two_commands_demo.py", note: "Bạn bấm RUN để kiểm chứng đoạn code. Cho sẵn 14, 5 và mã 1; bài này không đọc INPUT. Nhánh 1 cộng hai số, nên OUTPUT đúng là `19`.", expectOut: /^19$/ },
  fix: { starter: code(["from old_computer import say_num","","first = 20","second = 8","command = 2","","if command == 2:","    result = first + second","elif command == 1:","    result = first - second","","say_num(result)"]), solution: code(["from old_computer import say_num","","first = 20","second = 8","command = 2","","if command == 1:","    result = first + second","elif command == 2:","    result = first - second","","say_num(result)"]), label: "p50_two_commands_fix.py", note: "ĐỀ BÀI: Cho sẵn `first = 20`, `second = 8` và `command = 2`; bài này không đọc INPUT. Mã 1 phải chọn phép cộng, mã 2 phải chọn phép trừ. Đổi hai điều kiện để OUTPUT đúng là `12`.", expectOut: /^12$/ },
  check: "Mã 1 dùng phép cộng; mã 2 dùng phép trừ. Điều kiện so `command`, còn thân nhánh tính với `first` và `second`.",
  quiz: ["Cho sẵn ba giá trị; đoạn code này không đọc INPUT:\n```python\nfirst = 9\nsecond = 4\ncommand = 1\n\nif command == 1:\n    result = first + second\nelif command == 2:\n    result = first - second\n\nsay_num(result)\n```\nMáy in gì?", ["`13`", "`5`", "`1`"]],
  apply: { starter: code(["from old_computer import read_num, say_num","","first = read_num(\"Số thứ nhất: \")","second = read_num(\"Số thứ hai: \")","command = read_num(\"Lệnh (chỉ nhập 1 hoặc 2; 1 cộng, 2 trừ): \")","","if command == 1:","    result = first + second","elif command == 2:","    result = first + second","","say_num(result)"]), solution: code(["from old_computer import read_num, say_num","","first = read_num(\"Số thứ nhất: \")","second = read_num(\"Số thứ hai: \")","command = read_num(\"Lệnh (chỉ nhập 1 hoặc 2; 1 cộng, 2 trừ): \")","","if command == 1:","    result = first + second","elif command == 2:","    result = first - second","","say_num(result)"]), label: "p50_two_commands_input.py", note: "ĐỀ BÀI: Chương trình đọc ba INPUT mẫu là `30`, `7` và mã `2`. Trong chặng này, mã lệnh luôn là `1` hoặc `2`. Sửa phép tính trong nhánh mã 2 để máy chọn phép trừ và tạo OUTPUT `23`.", sampleInput: ["30","7","2"], expectOut: /^23$/ },
  remember: "Tách mã lệnh khỏi dữ liệu: điều kiện chọn việc cần làm, còn thân nhánh thực hiện phép tính."
});
