import { code, makePython50Lesson } from "./python50-template.js";

const TICKET_PROGRAM = [
  "from old_computer import say",
  "",
  "speed = 65",
  "",
  "if speed <= 60:",
  "    say(\"KHÔNG BỊ PHẠT\")",
  "elif speed <= 80:",
  "    say(\"PHẠT NHẸ\")",
  "else:",
  "    say(\"PHẠT NẶNG\")",
];

export default makePython50Lesson(2, {
  subtitle: "phân loại tốc độ bằng ba khoảng không chồng nhau",
  machineName: "TRẠM BA MỨC",
  machineBlurb: "xếp mỗi tốc độ vào đúng một mức phạt",
  intro: "Trạm chia tốc độ thành ba khoảng rõ ràng. Bạn sẽ thử cả các giá trị nằm đúng tại mốc 60 và 80 để không bỏ sót ranh giới.",
  prior: ["Điều kiện nào nhận cả tốc độ đúng 60?", ["`speed <= 60`", "`speed < 60`", "`speed == 59`"]],
  teach: "Ba khoảng không chồng nhau: từ 60 trở xuống thì không bị phạt, từ 61 đến 80 bị phạt nhẹ, từ 81 trở lên bị phạt nặng.",
  demo: {
    starter: code(TICKET_PROGRAM),
    label: "p50_ticket_demo.py",
    note: "Bạn bấm RUN để kiểm chứng đoạn code. Cho sẵn tốc độ 65; bài này không đọc INPUT. Số 65 thuộc khoảng từ 61 đến 80, nên OUTPUT đúng là `PHẠT NHẸ`.",
    expectOut: /^PHẠT NHẸ$/,
  },
  fix: {
    starter: code(TICKET_PROGRAM.map(line => line === "speed = 65" ? "speed = 80" : line === "elif speed <= 80:" ? "elif speed < 80:" : line)),
    solution: code(TICKET_PROGRAM.map(line => line === "speed = 65" ? "speed = 80" : line)),
    label: "p50_ticket_boundary_fix.py",
    note: "ĐỀ BÀI: Cho sẵn tốc độ đúng 80; bài này không đọc INPUT. Sửa điều kiện của nhánh giữa để 80 thuộc mức phạt nhẹ. OUTPUT đúng là `PHẠT NHẸ`.",
    expectOut: /^PHẠT NHẸ$/,
  },
  check: "Sau khi nhánh `speed <= 60` không chạy, nhánh `speed <= 80` nhận các số từ 61 đến 80. Nhánh `else` nhận mọi số lớn hơn 80.",
  quiz: ["Với các nhánh `speed <= 60`, `speed <= 80`, rồi `else`, tốc độ 81 tạo OUTPUT nào?", ["`PHẠT NẶNG`", "`PHẠT NHẸ`", "`KHÔNG BỊ PHẠT`"]],
  apply: {
    starter: code([
      "from old_computer import read_num, say",
      "",
      "speed = read_num(\"Tốc độ: \")",
      "",
      "if speed <= 60:",
      "    say(\"KHÔNG BỊ PHẠT\")",
      "elif speed < 80:",
      "    say(\"PHẠT NHẸ\")",
      "else:",
      "    say(\"PHẠT NẶNG\")",
    ]),
    solution: code([
      "from old_computer import read_num, say",
      "",
      "speed = read_num(\"Tốc độ: \")",
      "",
      "if speed <= 60:",
      "    say(\"KHÔNG BỊ PHẠT\")",
      "elif speed <= 80:",
      "    say(\"PHẠT NHẸ\")",
      "else:",
      "    say(\"PHẠT NẶNG\")",
    ]),
    label: "p50_ticket_input.py",
    note: "ĐỀ BÀI: Chương trình đọc INPUT mẫu là tốc độ `80`. Sửa điều kiện của nhánh giữa để khoảng 61 đến 80 đều thuộc mức phạt nhẹ. OUTPUT đúng là `PHẠT NHẸ`.",
    sampleInput: "80",
    expectOut: /^PHẠT NHẸ$/,
  },
  remember: "Viết các khoảng theo thứ tự và thử giá trị ngay dưới, đúng tại, ngay trên mỗi mốc để phát hiện chỗ bỏ sót.",
});
