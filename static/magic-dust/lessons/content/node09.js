export default {
  index: 9,
  title: "Logic Gates: and or not",
  subtitle: "ghép điều kiện bằng `and`, `or`, `not` — và dấu `!=`",
  bundle: { art: "assets/future-packet.webp", name: "LOGIC PACKET" },
  machine: { art: "assets/future-machine.webp", name: "LOGIC MODULE", blurb: "module gắn thêm cho FUTURE MACHINE — nối nhiều điều kiện thành một luật rõ ràng" },
  modules: { old_computer: "../py/old_computer/__init__.py", camera_charm: "../py/camera_charm/__init__.py" },
  cells: [
    { npc: "Suỵt… trạm đá này có hai ổ khóa nằm chồng lên nhau. Một ổ đo sức mạnh, một ổ đọc số ngón tay." },
    { npc: "Chúa tể Vô Định để lại vết nứt logic ở đây: chỉ cần nối sai một chữ thôi là cổng mở nhầm ngay." },
    { quiz: { title: "Ôn đường cũ", questions: [{ q: "`power = 4`, rồi `result = power >= 3`. Nếu `if result:` thì máy đi vào nhánh nào?", a: ["Nhánh ĐÚNG, vì 4 từ 3 trở lên", "Nhánh SAI, vì 4 không bằng đúng 3", "Máy báo lỗi vì không giữ được True/False"], correct: 0 }] } },
    { npc: "Theo cách cũ, tụi mình có thể đặt một khối `if` bên trong một khối `if` khác: qua ổ sức mạnh trước, rồi mới tới ổ số ngón." },
    { npc: "Bạn bấm RUN xem cánh cổng hai ổ khóa hoạt động ra sao nhé." },
    { code: 'from camera_charm import lighten\nfrom old_computer import say\n\npower = 3\nfinger = 1\n\nif power >= 3:\n    if finger == 1:\n        lighten()\n        say("Cổng mở: đủ cả hai ổ khóa")\n    else:\n        say("Cổng đóng: sai số ngón")\nelse:\n    say("Cổng đóng: chưa đủ sức mạnh")\n', label: "two_lock_old_way.py", note: "ĐỀ BÀI\nBạn hãy bấm RUN để niệm thử đoạn code cũ. Nó kiểm tra sức mạnh trước, rồi kiểm tra số ngón sau; đủ cả hai thì cổng mới mở.", expectOut: /đủ cả hai/i, solution: 'from camera_charm import lighten\nfrom old_computer import say\n\npower = 3\nfinger = 1\n\nif power >= 3:\n    if finger == 1:\n        lighten()\n        say("Cổng mở: đủ cả hai ổ khóa")\n    else:\n        say("Cổng đóng: sai số ngón")\nelse:\n    say("Cổng đóng: chưa đủ sức mạnh")\n' },
    { gift: { glyph: "&&", name: "SCROLL OF BOTH", blurb: "`and` nối hai điều kiện; cả bên trái và bên phải đều phải ĐÚNG thì toàn bộ điều kiện mới ĐÚNG" } },
    { npc: "`and` đọc là \"và\". Dòng `if power >= 3 and finger == 1:` nghĩa là: cổng mở khi sức mạnh từ 3 trở lên VÀ số ngón tay bằng 1. Chỉ thiếu một bên thôi là cổng đóng." },
    { code: 'from old_computer import say\n\npower = 3\nfinger = 1\nif power >= 3 and finger == 1:\n    say("3 và 1: MỞ")\nelse:\n    say("3 và 1: ĐÓNG")\n\npower = 3\nfinger = 2\nif power >= 3 and finger == 1:\n    say("3 và 2: MỞ")\nelse:\n    say("3 và 2: ĐÓNG")\n\npower = 2\nfinger = 1\nif power >= 3 and finger == 1:\n    say("2 và 1: MỞ")\nelse:\n    say("2 và 1: ĐÓNG")\n\npower = 2\nfinger = 2\nif power >= 3 and finger == 1:\n    say("2 và 2: MỞ")\nelse:\n    say("2 và 2: ĐÓNG")\n', label: "and_four_cases.py", note: "ĐỀ BÀI\nBảng này thử đủ 4 trường hợp: cả hai đúng, chỉ sức mạnh đúng, chỉ số ngón đúng, và cả hai đều sai. Bạn bấm RUN rồi nhìn xem `and` mở cổng ở trường hợp nào.", expectOut: { all: [/3 và 1: MỞ/i, /3 và 2: ĐÓNG/i, /2 và 1: ĐÓNG/i, /2 và 2: ĐÓNG/i] }, solution: 'from old_computer import say\n\npower = 3\nfinger = 1\nif power >= 3 and finger == 1:\n    say("3 và 1: MỞ")\nelse:\n    say("3 và 1: ĐÓNG")\n\npower = 3\nfinger = 2\nif power >= 3 and finger == 1:\n    say("3 và 2: MỞ")\nelse:\n    say("3 và 2: ĐÓNG")\n\npower = 2\nfinger = 1\nif power >= 3 and finger == 1:\n    say("2 và 1: MỞ")\nelse:\n    say("2 và 1: ĐÓNG")\n\npower = 2\nfinger = 2\nif power >= 3 and finger == 1:\n    say("2 và 2: MỞ")\nelse:\n    say("2 và 2: ĐÓNG")\n' },
    { npc: "Giờ bạn tự chữa cổng nhé. Luật khắc trên đá ghi: cần CẢ HAI, sức mạnh từ 3 trở lên và đúng 2 ngón tay." },
    { npc: "Đoạn code mới chỉ kiểm tra sức mạnh, nên người giơ sai số ngón vẫn lọt qua." },
    { code: 'from old_computer import say\n\npower = 4\nfinger = 1\n\n# Đến lượt của bạn: cổng cần CẢ HAI, power >= 3 và finger == 2\nif power >= 3:\n    say("Cổng mở nhầm")\nelse:\n    say("Cổng đóng đúng")\n', label: "and_guard.py", note: "ĐỀ BÀI\nBạn hãy sửa dòng `if` để cổng chỉ mở khi `power >= 3` VÀ `finger == 2`. Với dữ liệu đang có, finger là 1 nên kết quả đúng phải là cổng đóng.", expectOut: /cổng đóng đúng/i, solution: 'from old_computer import say\n\npower = 4\nfinger = 1\n\nif power >= 3 and finger == 2:\n    say("Cổng mở đúng")\nelse:\n    say("Cổng đóng đúng")\n' },
    { checkpoint: { text: "`A and B` chỉ ĐÚNG khi cả điều kiện A và điều kiện B cùng ĐÚNG; nếu một bên SAI thì toàn bộ điều kiện SAI." } },
    { quiz: { title: "Cổng cần cả hai", questions: [{ q: "`power = 2`, `finger = 1`. Với `if power >= 3 and finger == 1:`, cổng sẽ thế nào?", a: ["Mở, vì finger bằng 1", "Đóng, vì `and` cần cả hai bên đúng mà sức mạnh chưa đủ 3", "Máy báo lỗi vì không được dùng hai điều kiện"], correct: 1 }] } },
    { npc: "Ổ khóa kế bên lại khác hẳn. Nó nhận một trong hai tín hiệu tay bí mật: 1 ngón hoặc 3 ngón." },
    { npc: "Nếu dùng `and`, mình đang bắt một bàn tay vừa là 1 vừa là 3 cùng lúc — chuyện đó không thể xảy ra." },
    { gift: { glyph: "||", name: "SCROLL OF EITHER", blurb: "`or` nối hai điều kiện; chỉ cần một bên ĐÚNG thì toàn bộ điều kiện đã ĐÚNG" } },
    { code: 'from old_computer import say\n\nfinger = 1\nif finger == 1 and finger == 3:\n    say("finger 1, dùng and: MỞ")\nelse:\n    say("finger 1, dùng and: ĐÓNG")\nif finger == 1 or finger == 3:\n    say("finger 1, dùng or: MỞ")\nelse:\n    say("finger 1, dùng or: ĐÓNG")\n\nfinger = 3\nif finger == 1 and finger == 3:\n    say("finger 3, dùng and: MỞ")\nelse:\n    say("finger 3, dùng and: ĐÓNG")\nif finger == 1 or finger == 3:\n    say("finger 3, dùng or: MỞ")\nelse:\n    say("finger 3, dùng or: ĐÓNG")\n', label: "and_or_same_fingers.py", note: "ĐỀ BÀI\nBạn hãy bấm RUN để so cùng hai giá trị finger là 1 và 3. `and` bắt cả hai điều kiện đúng cùng lúc; `or` chỉ cần một trong hai điều kiện đúng.", expectOut: { all: [/finger 1, dùng and: ĐÓNG/i, /finger 1, dùng or: MỞ/i, /finger 3, dùng and: ĐÓNG/i, /finger 3, dùng or: MỞ/i] }, solution: 'from old_computer import say\n\nfinger = 1\nif finger == 1 and finger == 3:\n    say("finger 1, dùng and: MỞ")\nelse:\n    say("finger 1, dùng and: ĐÓNG")\nif finger == 1 or finger == 3:\n    say("finger 1, dùng or: MỞ")\nelse:\n    say("finger 1, dùng or: ĐÓNG")\n\nfinger = 3\nif finger == 1 and finger == 3:\n    say("finger 3, dùng and: MỞ")\nelse:\n    say("finger 3, dùng and: ĐÓNG")\nif finger == 1 or finger == 3:\n    say("finger 3, dùng or: MỞ")\nelse:\n    say("finger 3, dùng or: ĐÓNG")\n' },
    { code: 'from old_computer import say\n\nfinger = 3\n\n# Đến lượt của bạn: cổng chấp nhận 1 ngón HOẶC 3 ngón, không cần cả hai\nif finger == 1 and finger == 3:\n    say("Cổng mở")\nelse:\n    say("Cổng đóng nhầm")\n', label: "or_gate_fix.py", note: "ĐỀ BÀI\nLuật ghi: chỉ cần 1 ngón HOẶC 3 ngón. Bạn hãy đổi chữ nối điều kiện cho đúng. Với `finger = 3`, cổng phải mở.", expectOut: /cổng mở/i, solution: 'from old_computer import say\n\nfinger = 3\n\nif finger == 1 or finger == 3:\n    say("Cổng mở")\nelse:\n    say("Cổng đóng nhầm")\n' },
    { checkpoint: { text: "`A or B` ĐÚNG khi ít nhất một trong hai điều kiện ĐÚNG; chỉ khi cả A và B đều SAI thì toàn bộ điều kiện mới SAI." } },
    { quiz: { title: "Cổng chọn một trong hai", questions: [{ q: "`finger = 2`. Với `if finger == 1 or finger == 3:`, cổng sẽ thế nào?", a: ["Mở, vì `or` luôn mở", "Đóng, vì finger không phải 1 cũng không phải 3", "Máy tự đổi 2 thành 3"], correct: 1 }] } },
    { npc: "Tới đây tụi mình có hai chữ nối: CẢ HAI dùng `and`, MỘT TRONG HAI dùng `or`. Phần tiếp theo không nối thêm điều kiện; nó đảo kết quả đúng/sai đang có." },
    { npc: "Còn một chữ nữa dùng để đảo giá trị True/False. `not` đọc là \"không\". Nếu điều kiện `finger == 1` đang ĐÚNG, thêm `not` phía trước sẽ biến nó thành SAI; nếu nó đang SAI, `not` biến thành ĐÚNG." },
    { gift: { glyph: "!", name: "SCROLL OF FLIP", blurb: "`not` đảo một kết quả True/False; `!=` là cách viết gọn của \"không bằng\"" } },
    { code: 'from old_computer import say\n\nfinger = 1\nif not (finger == 1):\n    say("finger 1: KHÔNG phải 1")\nelse:\n    say("finger 1: đúng là 1")\n\nfinger = 2\nif not (finger == 1):\n    say("finger 2: KHÔNG phải 1")\nelse:\n    say("finger 2: đúng là 1")\n', label: "not_flip.py", note: "ĐỀ BÀI\nBạn hãy bấm RUN để xem `not (finger == 1)` đảo kết quả của điều kiện `finger == 1` như thế nào.", expectOut: { all: [/finger 1: đúng là 1/i, /finger 2: KHÔNG phải 1/i] }, solution: 'from old_computer import say\n\nfinger = 1\nif not (finger == 1):\n    say("finger 1: KHÔNG phải 1")\nelse:\n    say("finger 1: đúng là 1")\n\nfinger = 2\nif not (finger == 1):\n    say("finger 2: KHÔNG phải 1")\nelse:\n    say("finger 2: đúng là 1")\n' },
    { npc: "Khi chỉ muốn hỏi \"không bằng\", Kotopia có dấu gọn hơn: `!=`. Dòng `finger != 1` nghĩa y hệt `not (finger == 1)`, nhưng ngắn và dễ đọc hơn." },
    { code: 'from old_computer import say\n\nfinger = 2\n\n# Đến lượt của bạn: viết gọn not (finger == 1) thành finger != 1\nif not (finger == 1):\n    say("Đi lối phụ")\nelse:\n    say("Ở lại cổng chính")\n', label: "not_equal_shortcut.py", note: "ĐỀ BÀI\nBạn hãy sửa dòng `if` để dùng dấu `!=` thay cho `not (finger == 1)`. Với `finger = 2`, máy vẫn phải in ra lối phụ.", expectOut: /lối phụ/i, solution: 'from old_computer import say\n\nfinger = 2\n\nif finger != 1:\n    say("Đi lối phụ")\nelse:\n    say("Ở lại cổng chính")\n' },
    { checkpoint: { text: "`not A` đảo kết quả của điều kiện A; `x != y` là cách viết gọn cho `not (x == y)`, nghĩa là x không bằng y." } },
    { quiz: { title: "Đảo ngược điều kiện", questions: [{ q: "Đọc đoạn code này:\n```python\nfinger = 1\nif finger != 1:\n    say(\"lối phụ\")\nelse:\n    say(\"cổng chính\")\n```\nMáy in gì?", a: ["lối phụ", "cổng chính", "Cả hai dòng"], correct: 1 }] } },
    { npc: "Giờ Pip cho bạn xem hai vết nứt kinh điển. Vết nứt thứ nhất: luật nói CẦN CẢ HAI, nhưng đoạn code dùng `or`, nên quá dễ dãi. Người chỉ có một bên đúng vẫn lẻn qua." },
    { code: 'from old_computer import say\n\npower = 2\nfinger = 1\n\n# LUẬT TRÊN CỔNG: cần CẢ HAI, power >= 3 và finger == 1\n# Đến lượt của bạn: chữ nối bên dưới đang quá dễ dãi\nif power >= 3 or finger == 1:\n    say("Lọt qua nhầm")\nelse:\n    say("Chặn lại đúng")\n', label: "too_lenient_or.py", note: "ĐỀ BÀI\nLuật ghi cần CẢ HAI, nhưng dòng `if` đang dùng `or`. Bạn hãy đổi sang chữ nối đúng. Với `power = 2`, cổng phải chặn lại.", expectOut: /chặn lại đúng/i, solution: 'from old_computer import say\n\npower = 2\nfinger = 1\n\nif power >= 3 and finger == 1:\n    say("Lọt qua nhầm")\nelse:\n    say("Chặn lại đúng")\n' },
    { npc: "Vết nứt thứ hai ngược lại: luật nói chỉ cần MỘT trong hai, nhưng đoạn code dùng `and`, nên quá khó tính. Người giơ đúng 3 ngón vẫn bị giữ ngoài cổng." },
    { code: 'from old_computer import say\n\nfinger = 3\n\n# LUẬT TRÊN CỔNG: chỉ cần MỘT trong hai số ngón, finger == 1 hoặc finger == 3\n# Đến lượt của bạn: chữ nối bên dưới đang quá khó tính\nif finger == 1 and finger == 3:\n    say("Cổng mở đúng")\nelse:\n    say("Bị chặn nhầm")\n', label: "too_strict_and.py", note: "ĐỀ BÀI\nLuật ghi chỉ cần MỘT trong hai số ngón: 1 hoặc 3. Bạn hãy đổi sang chữ nối đúng. Với `finger = 3`, cổng phải mở.", expectOut: /cổng mở đúng/i, solution: 'from old_computer import say\n\nfinger = 3\n\nif finger == 1 or finger == 3:\n    say("Cổng mở đúng")\nelse:\n    say("Bị chặn nhầm")\n' },
    { npc: "Khi một luật có nhiều hơn hai mảnh, Pip luôn đặt ngoặc `()` quanh từng cụm. Tụi mình chưa cần học luật ưu tiên rối rắm đâu; cứ gom bằng ngoặc cho mắt người đọc và cỗ máy đều rõ." },
    { code: 'from camera_charm import fire_vortex, freeze\nfrom old_computer import say\n\npower = 3\nfinger = 3\n\nif (finger == 1 or finger == 3) and (power >= 3):\n    fire_vortex()\n    say("Cổng logic mở")\nelse:\n    freeze()\n    say("Cổng logic đóng")\n', label: "parentheses_sample.py", note: "ĐỀ BÀI\nBạn hãy bấm RUN và đọc dòng `if`: cụm `(finger == 1 or finger == 3)` đứng riêng, cụm `(power >= 3)` đứng riêng, rồi `and` nối hai cụm lại.", expectOut: /cổng logic mở/i, solution: 'from camera_charm import fire_vortex, freeze\nfrom old_computer import say\n\npower = 3\nfinger = 3\n\nif (finger == 1 or finger == 3) and (power >= 3):\n    fire_vortex()\n    say("Cổng logic mở")\nelse:\n    freeze()\n    say("Cổng logic đóng")\n' },
    { code: 'from old_computer import say\n\npower = 2\nfinger = 3\n\n# LUẬT: (finger là 1 hoặc 3) VÀ (power từ 3 trở lên)\n# Đến lượt của bạn: dòng if đang thiếu cụm sức mạnh\nif finger == 1 or finger == 3:\n    say("Mở nhầm")\nelse:\n    say("Đóng đúng")\n', label: "parentheses_guard.py", note: "ĐỀ BÀI\nBạn hãy sửa dòng `if` thành hai cụm có ngoặc: `(finger == 1 or finger == 3)` VÀ `(power >= 3)`. Với `power = 2`, cổng phải đóng.", expectOut: /đóng đúng/i, solution: 'from old_computer import say\n\npower = 2\nfinger = 3\n\nif (finger == 1 or finger == 3) and (power >= 3):\n    say("Mở nhầm")\nelse:\n    say("Đóng đúng")\n' },
    { checkpoint: { text: "Khi ghép từ hai điều kiện trở lên, đặt từng cụm trong ngoặc `()` rồi mới nối bằng `and`/`or`/`not`; cách này làm luật rõ và tránh hiểu nhầm." } },
    { quiz: { title: "Nhóm điều kiện bằng ngoặc", questions: [{ q: "Đọc đoạn code này:\n```python\npower = 2\nfinger = 3\nif (finger == 1 or finger == 3) and (power >= 3):\n    ...\n```\nCổng sẽ thế nào?", a: ["Mở, vì finger là 3", "Đóng, vì cụm sức mạnh chưa đúng nên toàn bộ điều kiện `and` sai", "Máy bỏ qua cụm trong ngoặc"], correct: 1 }] } },
    { npc: "Nó tới rồi. THE LOGIC SHADE — bóng mờ của Chúa tể Vô Định, chuyên ăn những luật quá dễ dãi hoặc quá khó tính. Mỗi vòng, nó đọc một luật bằng tiếng người;" },
    { npc: "bạn phải biến luật đó thành Mật Ngữ đúng." },
    { boss: { name: "THE LOGIC SHADE", glyph: "🌫️", hp: 900, baseDmg: 20, streakMul: [1, 1.5, 2], heal: 10, hearts: 3, rounds: [{ q: "Luật ghi: sức mạnh từ 3 trở lên VÀ số ngón bằng 1 thì mở. Dòng nào đúng?", a: ["if power >= 3 and finger == 1:", "if power >= 3 or finger == 1:", "if finger != 1:"], correct: 0 }, { q: "Luật ghi: 1 ngón HOẶC 3 ngón thì mở. Dòng nào đúng?", a: ["if finger == 1 and finger == 3:", "if finger == 1 or finger == 3:", "if not (finger == 3):"], correct: 1 }, { code: 'from old_computer import say\n\n# LUẬT: cần CẢ HAI, power >= 3 và finger == 1\npower = 2\nfinger = 1\n\nif power >= 3 or finger == 1:\n    say("Bóng lọt qua")\nelse:\n    say("Bóng bị chặn")\n', label: "shade_too_easy.py", note: "Bóng mờ đổi `and` thành `or`, làm luật quá dễ dãi. Bạn hãy sửa chữ nối để người thiếu sức mạnh bị chặn lại.", dmg: 30, expectOut: /bóng bị chặn/i, solution: 'from old_computer import say\n\npower = 2\nfinger = 1\n\nif power >= 3 and finger == 1:\n    say("Bóng lọt qua")\nelse:\n    say("Bóng bị chặn")\n' }, { q: "`finger = 2`. Với `if finger != 1:`, nhánh ĐÚNG hay SAI chạy?", a: ["Nhánh ĐÚNG, vì 2 không bằng 1", "Nhánh SAI, vì 2 chưa phải 3", "Máy báo lỗi vì dấu != không dùng với số"], correct: 0 }, { code: 'from old_computer import say\n\n# LUẬT: chỉ cần MỘT trong hai số ngón, finger == 1 hoặc finger == 3\nfinger = 3\n\nif finger == 1 and finger == 3:\n    say("Đường mở")\nelse:\n    say("Đường bị khóa")\n', label: "shade_too_strict.py", note: "Bóng mờ đổi `or` thành `and`, làm luật quá khó tính. Bạn hãy sửa chữ nối để `finger = 3` mở được đường.", dmg: 30, expectOut: /đường mở/i, solution: 'from old_computer import say\n\nfinger = 3\n\nif finger == 1 or finger == 3:\n    say("Đường mở")\nelse:\n    say("Đường bị khóa")\n' }, { q: "`power = 2`, `finger = 3`. Luật `(finger == 1 or finger == 3) and (power >= 3)` trả lời thế nào?", a: ["ĐÚNG, vì finger là 3", "SAI, vì cụm sức mạnh chưa đạt 3", "ĐÚNG, vì có ngoặc"], correct: 1 }, { code: 'from camera_charm import fire_vortex, freeze\nfrom old_computer import say\n\n# LUẬT: (finger là 1 hoặc 3) VÀ (sức mạnh từ 3 trở lên)\npower = 3\nfinger = 3\n\nif finger == 1 and finger == 3 and power >= 3:\n    fire_vortex()\n    say("Bóng tan")\nelse:\n    freeze()\n    say("Bóng còn đó")\n', label: "shade_final_lock.py", note: "ĐÒN KẾT LIỄU: Bóng mờ viết sai cụm số ngón. Bạn hãy sửa dòng `if` thành `(finger == 1 or finger == 3) and (power >= 3)`, rồi RUN để phá khóa.", dmg: 50, finisher: true, expectOut: /bóng tan/i, solution: 'from camera_charm import fire_vortex, freeze\nfrom old_computer import say\n\npower = 3\nfinger = 3\n\nif (finger == 1 or finger == 3) and (power >= 3):\n    fire_vortex()\n    say("Bóng tan")\nelse:\n    freeze()\n    say("Bóng còn đó")\n' }] } },
    { npc: "Bóng mờ tan rồi. Từ giờ, gặp luật nào ghi CẢ HAI thì bạn nghĩ tới `and`; ghi MỘT TRONG HAI thì nghĩ tới `or`; ghi KHÔNG thì dùng `not` hoặc `!=`. Hải đăng đang chờ bạn niêm phong đó." },
  ],
  ritual: { gesture: "✋", prompt: "GIƠ TAY NIÊM PHONG CỔNG LOGIC", theme: { motion: "comet", palette: { core: "#78b2a5", dust: "#e3f3dc", rune: "#f4c85a" }, glyphs: "and or not" } },
};
