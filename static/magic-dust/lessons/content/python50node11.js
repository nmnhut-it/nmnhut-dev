import { code, makePython50Lesson } from "./python50-template.js";
export default makePython50Lesson(11, {
  subtitle: "chuẩn hóa, đếm và kiểm tra chuỗi",
  machineName: "ỐNG KÍNH CHUỖI", machineBlurb: "đọc từng ký tự sau khi chuyển về chữ thường",
  intro: "Ống kính sẽ nhìn chuỗi theo hai cách: quét từng ký tự để đếm, rồi so chuỗi với thứ tự ngược để kiểm tra đối xứng.",
  prior: ["Cho `text = \"KoTo\"`. `text.lower()` tạo chuỗi nào?", ["`\"koto\"`", "`\"KOTO\"`", "`\"KoTo\"`"]],
  teach: "Chuyển về chữ thường trước khi so. Vòng `for char in text` đọc từng ký tự; `text[::-1]` tạo chuỗi theo thứ tự ngược.",
  demo: { starter: code(["from old_computer import say_num","","text = \"Kotopia\"","text = text.lower()","count = 0","","for char in text:","    if char == \"o\":","        count = count + 1","","say_num(count)"]), label: "p50_string_count_demo.py", note: "Bạn bấm RUN để kiểm chứng đoạn code. Cho sẵn chuỗi `Kotopia`; bài này không đọc INPUT. Sau khi đổi thành chữ thường, chương trình đếm được hai ký tự `o`, nên OUTPUT đúng là `2`.", expectOut: /^2$/ },
  fix: { starter: code(["from old_computer import say","","text = \"Level\"","text = text.lower()","","if text == text:","    say(\"ĐỐI XỨNG\")","else:","    say(\"KHÔNG ĐỐI XỨNG\")"]), solution: code(["from old_computer import say","","text = \"Level\"","text = text.lower()","","if text == text[::-1]:","    say(\"ĐỐI XỨNG\")","else:","    say(\"KHÔNG ĐỐI XỨNG\")"]), label: "p50_palindrome_fix.py", note: "ĐỀ BÀI: Cho sẵn chuỗi `Level`; bài này không đọc INPUT. Sửa vế phải của điều kiện để so với chuỗi theo thứ tự ngược. OUTPUT đúng là `ĐỐI XỨNG`.", expectOut: /^ĐỐI XỨNG$/ },
  check: "`lower()` giúp bỏ khác biệt chữ hoa và chữ thường. Một chuỗi đối xứng khi nó bằng chính chuỗi theo thứ tự ngược.",
  quiz: ["Đoạn code này gán `False` hay `True` vào `is_symmetric`?\n```python\ntext = \"Robot\".lower()\nis_symmetric = text == text[::-1]\n```", ["`False`, vì `robot` khác `tobor`", "`True`, vì hai chuỗi dài bằng nhau", "`True`, vì cả hai đều là chữ thường"]],
  apply: { starter: code(["from old_computer import read, say_num","","text = read(\"Nhập chuỗi: \")","text = text.lower()","vowel_count = 0","","for char in text:","    if char == \"a\":","        vowel_count = vowel_count + 1","","say_num(vowel_count)"]), solution: code(["from old_computer import read, say_num","","text = read(\"Nhập chuỗi: \")","text = text.lower()","vowel_count = 0","","for char in text:","    if char == \"a\" or char == \"e\" or char == \"i\" or char == \"o\" or char == \"u\":","        vowel_count = vowel_count + 1","","say_num(vowel_count)"]), label: "p50_vowels_input.py", note: "ĐỀ BÀI: Chương trình đọc INPUT mẫu là `Kotopia` và đã đổi chuỗi thành chữ thường. Mở rộng điều kiện để đếm các ký tự `a`, `e`, `i`, `o`, `u`. OUTPUT đúng là `4`.", sampleInput: "Kotopia", expectOut: /^4$/ },
  remember: "Chuẩn hóa chuỗi trước, rồi quét từng ký tự hoặc so với phiên bản viết ngược để trả lời câu hỏi cụ thể."
});
